const readline = require("readline");

const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question("Какая сейчас температура на улице? ", function(temp) {
  temp = Number(temp);

  if (temp <= 0) {
    console.log(" Очень холодно! Надень тёплую куртку, шапку и перчатки.");
  } else if (temp > 0 && temp <= 10) {
    console.log(" Прохладно. Лучше надеть куртку и шарф.");
  } else if (temp > 10 && temp <= 20) {
    console.log("20 && temp <= 30) {
    console.log(" Тепло! Можно надеть футболку и джинсы.");
  } else {
    console.log("Очень жарко! Надень лёгкую одежду и не забудь воду.");
  }

  rl.close();
});
